import React from 'react';

console.log('Basic Extension Loaded');

// Example of using the global registry (if types were fully set up)
// window.ExtensionExtension.pluginManager.register(...)

export default function init() {
    console.log('Basic Extension Init');
}
